# 인증·신규 기능 검증 체크리스트

명세 r11 반영(PR #5)의 수동 검증 목록입니다. 자동화 테스트가 없으므로 이 문서가 유일한 회귀 그물입니다.

- 대상 커밋: `7d5e509` (리뷰 지적 반영분 포함)
- 관련 PR: [#5](https://github.com/OLD-CREATER-CREW/childcare-FE/pull/5)
- 실패한 항목은 **번호로** 알려 주세요(예: "B-3 실패, 400 떨어짐"). 각 항목에 의심 지점을 적어 뒀습니다.

---

## 0. 준비

### A파트(목 모드)용

```bash
# .env.local
NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_USE_MOCK=true
NEXT_PUBLIC_APP_ENV=development
```

- [ ] **0-1** 위 설정으로 `npm run dev` → 콘솔에 `[MSW] worker started`가 뜬다
  - 안 뜨면: `NEXT_PUBLIC_USE_MOCK`이 정확히 `"true"`인지 확인. 이번에 기본값을 **fail-safe로 반전**해서, 빈 값이나 오타면 목이 꺼지고 실 서버로 나갑니다(의도된 동작)

목 계정 — 비밀번호는 아무 값이나 됩니다(목은 검증하지 않음):

| 아이디          | 역할     | 용도                     |
| --------------- | -------- | ------------------------ |
| `demo_director` | 원장     | 원장 전용 화면·버튼 확인 |
| `demo_teacher`  | 보육교사 | 역할 분기 확인           |

> 아이디에 `director`가 **포함**되면 원장으로 로그인됩니다.

### B·C파트(실 서버)용

```bash
# .env.local
NEXT_PUBLIC_API_BASE_URL=https://api.child-care.site
NEXT_PUBLIC_USE_MOCK=false
NEXT_PUBLIC_APP_ENV=development
```

- [ ] **0-2** 검증 전용 기관·원장 계정 확보 (실계정 대신 새로 만들기 권장)
- [ ] **0-3** 그 원장으로 교사 계정 하나 생성 (역할 분기 확인용)

---

## A. 목 모드 UI 검증 — 계정 없이 지금 바로 가능

### A-1. 역할 분기 (명세 1.2.1)

- [ ] **A-1-1** `demo_director`로 로그인 → 좌측 「원 관리」에 **아동 관리 + 계정 관리 둘 다** 보인다
- [ ] **A-1-2** 아동 관리 우상단에 「＋ 원아 등록」이 보인다
- [ ] **A-1-3** 로그아웃 → `demo_teacher`로 로그인 → 「계정 관리」 메뉴가 **안 보인다**
- [ ] **A-1-4** 교사 상태에서 주소창에 `/users` 직접 입력 → "원장 권한이 필요한 기능입니다" 카드만 뜨고, **네트워크 탭에 `/api/users` 요청이 0건**
  - 요청이 나가면: `useUsers(activeOnly, isDirector)`의 `enabled`가 안 걸린 것
- [ ] **A-1-5** 교사로 아동 관리 → 「원아 등록」·「퇴소 처리」 버튼이 없다. 반·특이사항 수정 후 저장은 **성공**한다(EP-041은 교사도 가능)

### A-2. 인적사항 데이터 손실 방지 ⭐ Blocker 2 수정 확인

- [ ] **A-2-1** 원장으로 아동 하나를 열어 생년월일·반·입소일·특이사항을 **모두 채워** 저장
- [ ] **A-2-2** 개발자도구 Network → `/api/children/*` 요청 차단(Block request URL) 후 같은 행을 다시 연다
- [ ] **A-2-3** **폼이 아니라 오류 카드 + 재시도 버튼**이 뜬다
  - 빈 폼이 뜨면: 수정이 안 들어간 것. 그 상태로 저장하면 채운 값이 전부 지워집니다
- [ ] **A-2-4** 차단 해제 → 재시도 → 값이 그대로 보인다
- [ ] **A-2-5** 특이사항만 고쳐 저장 → Network에서 PATCH 본문에 **`memo` 키 하나만** 있다
  - 다른 키가 함께 나가면: `changedFields`가 안 걸린 것

### A-3. 401 자동 갱신 ⭐ Blocker 5 수정 확인

콘솔에서 스위치를 켜고 **홈이나 아동 관리로 이동**하면 됩니다(`/api/children`을 부르는 화면).

- [ ] **A-3-1** `localStorage.setItem("childcare.mock.force401", "1")` → 화면 이동
  - **기대**: 로그인 화면으로 튀지 않고 데이터가 정상 표시. Network에 `POST /api/auth/refresh` **1건** + 원 요청 재시도 1건
- [ ] **A-3-2** `localStorage.setItem("childcare.mock.force401", "all")` → 화면 이동
  - **기대**: 갱신도 401 → 로그인 화면으로. `localStorage`에 `childcare.refresh_token`이 남아 있지 않다
- [ ] **A-3-3** 홈처럼 요청이 여러 개 동시에 나가는 화면에서 A-3-1 반복 → **refresh는 1건만** 나간다
  - 여러 건이면: 갱신 프라미스 공유가 깨진 것

> 끝나면 `localStorage.removeItem("childcare.mock.force401")`로 꺼 주세요.

### A-4. 계정 관리 잠금 방지 (명세 1.2.2)

- [ ] **A-4-1** 원장으로 `/users` → 목록에 `demo_director`·`demo_teacher`·`lee_boyuk`(잠김)이 보인다
- [ ] **A-4-2** 「사용중인 계정만 보기」 체크 → `lee_boyuk`이 빠진다
- [ ] **A-4-3** 자기 행(`demo_director`, "(나)" 표시)을 열면 **「계정 잠금」 버튼이 없다**
- [ ] **A-4-4** 자기 행의 역할 드롭다운에 **「보육교사」 선택지가 없고**, "기관에 원장이 최소 한 명은 있어야 합니다" 안내가 뜬다(활성 원장이 혼자일 때)
- [ ] **A-4-5** `lee_boyuk`을 열어 「사용중으로 되돌리기」 → 목록에서 「사용중」으로 바뀐다
- [ ] **A-4-6** 계정 생성에서 아이디에 대문자·한글 입력 → 요청을 보내지 않고 형식 안내가 뜬다
- [ ] **A-4-7** 비밀번호를 7자로 입력 → 요청을 보내지 않고 "8자 이상" 안내가 뜬다

### A-5. 비밀번호 변경 (EP-049)

- [ ] **A-5-1** 좌측 「계정 > 비밀번호 변경」 → 현재 비밀번호를 **비운 채** 제출 → "현재 비밀번호를 입력해 주세요."(서버 요청 없음)
- [ ] **A-5-2** 새 비밀번호를 현재와 같게 입력 → "새 비밀번호가 현재 비밀번호와 같습니다."
- [ ] **A-5-3** 정상 변경 → "다른 기기 N곳에서 로그아웃되었습니다" 토스트 → **다른 화면으로 이동해도 401이 안 난다**
  - 401이 나면: 응답의 새 액세스 토큰 교체가 실패한 것
- [ ] **A-5-4** 모달을 닫았다 다시 열면 입력칸이 **비어 있다**

### A-6. 세션 지속·정리

- [ ] **A-6-1** 로그인 상태에서 F5 → "세션 확인 중…" 후 로그인 화면을 **거치지 않고** 원래 화면으로 복귀
- [ ] **A-6-2** 로그아웃 → 뒤로가기로 `/dashboard` 진입 시도 → `/login`으로 되돌아가고 `localStorage`에 토큰이 없다
- [ ] **A-6-3** 로그아웃 시 Network에 **401이 남지 않는다**
  - 401이 뜨면: 캐시 정리 시점 문제(`removeQueries` 수정이 안 들어간 것)
- [ ] **A-6-4** 탭 두 개를 열어 둔 상태에서 한쪽에서 로그아웃 → **다른 탭도** 로그인 화면으로 돌아간다

### A-7. 표시 정합성

- [ ] **A-7-1** 원장으로 로그인 → 상단바가 「데모 원장 · 원장」, 홈 서브헤더의 이름도 **같은 사람**이다
  - 다르면: 하드코딩 상수가 남은 것
- [ ] **A-7-2** 아동 관리에서 「퇴소 아동 보기」를 켰다 끄기 → 목록이 0명이 되지 않는다(반 필터가 사라진 값을 가리키지 않는다)
- [ ] **A-7-3** 지표 대시보드에 「확정자별 지표」 표와 귀속 불가 건수 안내가 보인다

---

## B. 실 서버 API 계약 — 계정 필요 (제가 대신 실행 가능)

`{{TOKEN}}`은 로그인 응답의 `access_token`입니다.

- [ ] **B-1** 로그인 → 응답 본문에 `access_token`·`refresh_token`·`user` 4종이 온다
- [ ] **B-2** `Authorization: Bearer {{TOKEN}}`으로 `GET /api/auth/me` → 200
- [ ] **B-3** ⭐ **아동 등록** — 이름만 넣고 `POST /api/children` → **201**, 응답의 `enrolled_at`이 **오늘 날짜로 채워져 있다**
  - **이번 수정(Blocker 3)의 핵심 확인 지점입니다.** 400이 나거나 `enrolled_at`이 `null`이면 생성 본문에서 빈 값이 여전히 `null`로 나가는 것 — 목은 이 결함을 가립니다
- [ ] **B-4** 같은 이름으로 한 번 더 등록 → `409 DUPLICATE_CHILD`
- [ ] **B-5** `GET /api/children?status=all` / `?status=withdrawn` → 200 (아직 한 번도 확인 못 한 쿼리)
- [ ] **B-6** `DELETE /api/children/{id}` → 200, `status: "withdrawn"`. 한 번 더 → `409 ALREADY_WITHDRAWN`
- [ ] **B-7** `PATCH /api/children/{id}` `{"status":"enrolled"}` → 200, `withdrawn_at`이 `null`로 지워진다
- [ ] **B-8** `PATCH /api/children/{id}` `{"memo":"수정"}` → 200, **다른 필드가 그대로 유지된다**(부분 수정 규약)
- [ ] **B-9** 교사 토큰으로 `POST /api/children` → `403 FORBIDDEN`
- [ ] **B-10** 교사 토큰으로 `GET /api/users` → `403 FORBIDDEN`
- [ ] **B-11** ⚠️ 교사 토큰으로 `PATCH /api/children/{id}` `{"status":"withdrawn"}` → **403이 나와야 정상**
  - **200이 나오면 서버 구멍입니다.** 명세 EP-041 오류표에 403 행이 없어 백엔드가 막지 않을 가능성이 높습니다. FE는 경로를 좁혀 뒀지만 서버가 최후 방어선이어야 합니다 → 백엔드에 전달 필요

---

## C. 실 서버 UI — 계정 필요

- [ ] **C-1** 회원가입(SCR-018)으로 새 기관 생성 → 로그인 화면을 거치지 않고 바로 홈으로, 상단바에 새 기관명이 뜬다
- [ ] **C-2** 같은 기관명으로 재가입 → `DUPLICATE_CENTER`가 **어린이집 이름 칸 아래**에 붙는다
- [ ] **C-3** 같은 아이디로 재가입 → `DUPLICATE_USERNAME`이 **아이디 칸 아래**에 붙는다
  - 폼 전체 오류로 뭉뚱그려지면: `ERROR_FIELD` 매핑 버그
- [ ] **C-4** A파트의 A-1 ~ A-7을 실 서버에서 반복 (목이 가리던 차이가 여기서 드러납니다)
- [ ] **C-5** 아동 하나를 퇴소 → 하루 기록·알림장·사진함·관찰 화면을 차례로 열어 **퇴소 아동만 빠지고 나머지 기록은 그대로**인지 확인 → 재원 복구 후 다시 나타나는지도 확인
- [ ] **C-6** 성별을 비운 채 등록한 아동이 목록에 **"—"** 로 보인다(「남」으로 뜨면 안 됨)
- [ ] **C-7** ⭐ **토큰 만료 자동 복구** — 로그인 후 **15분 이상 방치** → 아무 화면 클릭
  - **기대**: 로그인 화면으로 튀지 않고, Network에 `refresh` 1건 + 재시도 1건
- [ ] **C-8** ⭐ **갱신 불가 시 세션 유지** (Blocker 4 수정 확인)
  - C-7 상태(15분 경과)에서 개발자도구 Network → **`/api/auth/refresh`만 차단** → 화면 이동
  - **기대**: 오류는 뜨지만 **로그인 화면으로 튕기지 않고**, `localStorage`의 `childcare.refresh_token`이 **남아 있다**
  - 차단 해제 후 화면 이동 → 그대로 이어서 사용 가능
  - 토큰이 지워졌으면: 갱신 실패 3값 구분이 안 들어간 것

---

## 결과 기록

| 파트           | 통과   | 실패  | 미실행 |
| -------------- | ------ | ----- | ------ |
| A (목 UI)      |        |       | 26     |
| **B (실 API)** | **14** | **1** | 0      |
| C (실 UI)      |        |       | 8      |

### B파트 실행 결과 — 2026-08-04, 커밋 `869e874`

검증 기관 `QA검증원_test1`을 새로 만들어 실행했습니다. 계정은 `test1`(원장) · `test1_teacher`(교사).

| #                                        | 결과 | 비고                               |
| ---------------------------------------- | ---- | ---------------------------------- |
| B-1 로그인 → 토큰 2종 + user             | ✅   |                                    |
| B-2 Bearer로 `/auth/me`                  | ✅   |                                    |
| B-3 이름만으로 등록                      | ✅   | `enrolled_at`이 오늘로 자동 채워짐 |
| B-4 동명 재등록 409                      | ✅   | `DUPLICATE_CHILD`                  |
| B-5 `status` 쿼리 3종                    | ✅   | all/withdrawn/enrolled 모두 200    |
| B-6 퇴소 + 중복 409                      | ✅   | `ALREADY_WITHDRAWN`                |
| B-7 퇴소 취소                            | ✅   | `withdrawn_at`이 null로 지워짐     |
| B-8 부분 수정                            | ✅   | `memo`만 보내면 나머지 유지        |
| B-9 교사 → 아동 등록                     | ✅   | 403                                |
| B-10 교사 → 계정 목록                    | ✅   | 403                                |
| **B-11 교사 → `PATCH status=withdrawn`** | ❌   | **200 — 서버가 막지 않는다**       |
| B-14 EP-050 갱신                         | ✅   | 리프레시 토큰 회전 없음 확인       |

**추가 확인 — 기관 격리**: 새로 만든 기관의 아동 목록이 `total=0`으로 시작. `center_id`가 기존 값과 겹쳐도 데이터는 격리됩니다.

### ❌ B-11 — 백엔드 전달 필요

교사 토큰으로 `PATCH /api/children/{id}` `{"status":"withdrawn"}` → **200**. 퇴소(EP-042)는 원장 전용인데, EP-041의 `status` 필드로 같은 결과를 만들 수 있습니다.

- 프론트는 `ChildProfileInput`에서 `status`를 빼 경로를 좁혔지만, **서버가 최후 방어선이어야 합니다**
- 명세 EP-041 오류표에 `403` 행이 없어 백엔드가 열어 둔 것으로 보입니다 → 명세 1.2.1 권한표에 행 추가 + 서버 제한 요청

### 리뷰 판정 정정 2건 (실측 결과)

| 리뷰 지적                                                           | 실측                                                                                                 | 판정                                                           |
| ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| Blocker 3 — 생성 시 명시적 `null`을 보내면 400이 나거나 입소일이 빔 | **201 + `enrolled_at` 자동 채움.** 서버가 `null`을 "생략"으로 관대하게 처리                          | **과장** — 옛 코드도 동작했다. 수정은 명세 의미에 더 맞아 유지 |
| Blocker 2 — 빈 폼 저장 시 `null`이 서버 값을 지운다                 | 전 필드를 채운 아동에 `null` PATCH → **`birth`·`class_name`·`gender`·`memo`·`enrolled_at` 5개 삭제** | **확인됨.** 가장 심각한 지적이 맞았다                          |

> Blocker 1(목 fail-open)도 앞서 과장으로 정정했습니다. 5건 중 2건이 과장이었고, 데이터 손실(B2)·강제 로그아웃(B4)·검증 경로 부재(B5)는 유효합니다.

### C파트용으로 남겨 둔 데이터

| 구분 | 값                                                                                                   |
| ---- | ---------------------------------------------------------------------------------------------------- |
| 원장 | `test1`                                                                                              |
| 교사 | `test1_teacher`                                                                                      |
| 아동 | `가상아동_검증`(재원, **성별 미입력** → C-6용) · `가상아동_옛코드`(퇴소) · `가상아동_손실확인`(퇴소) |

퇴소 아동이 2명 있어 C-5(퇴소 필터)와 C-6(성별 `—` 표시)을 바로 확인할 수 있습니다.
